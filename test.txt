int a, b;

int fn(int c, int d) { 
    i32 middle = c * d;
    if (middle == 1) {
        return middle / (c + d);
    } else if (middle == 2) {
        return middle * (c + d);
    } else {
        return 3;
    }
}